# Disable the WebSocket hot-reload client overlay
# This removes the repeated "WebSocket connection to ws://localhost:3000/ws failed" warnings
FAST_REFRESH=false
WDS_SOCKET_HOST=localhost
WDS_SOCKET_PORT=3000
